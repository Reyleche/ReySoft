const { Pool } = require('pg');

// AQUÍ VA TU CONFIGURACIÓN DE POSTGRESQL
const pool = new Pool({
  user: 'postgres',
  host: 'localhost',
  database: 'db_cococana', 
  password: 'rey', 
  port: 5432,
});

pool.on('connect', () => {
  console.log('✅ Conexión exitosa a la Base de Datos de Coco y Caña');
});

module.exports = pool;